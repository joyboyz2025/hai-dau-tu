# CLAUDE.md — Hải Đầu Tư (runbook)

Web app nội bộ cho gia đình: tổng hợp nhận định đầu tư từ YouTube (Thái Phạm, AzFin, Chứng khoán 5 phút) thành feed có cấu trúc + điểm mua/bán + bảng điểm chuyên gia. Repo: `joyboyz2025/hai-dau-tu`, deploy: **Vercel auto-deploy khi push lên main**.

## Kiến trúc (hiện tại — bản trong design_handoff_hai_dau_tu/CLAUDE.md đã lỗi thời)

```
design_handoff_hai_dau_tu/
  Hai Dau Tu.dc.html   # Toàn bộ app 1 file (DCLogic engine: {{ }}, sc-if, sc-for, renderVals())
  data.js              # window.HDT_DATA — TOÀN BỘ nội dung (JSON, có header comment)
  support.js           # engine — không sửa
  manifest.json, icons/  # PWA
scan_channels.py       # Quét kênh: đọc sheet "Kênh" → yt-dlp liệt kê video mới → thêm vào Excel + tải transcript
get_transcript.py      # Tải transcript 1 video (yt-dlp + cookie Chrome — video hội viên cần Chrome login đúng tài khoản Platinum)
data/transcripts/<id>.json   # transcript đã tải (gitignore — chỉ có trên máy local)
Theo doi video-post kenh dau tu.xlsx  # sheet "Kênh" (danh bạ kênh) + "Video-Post mới" (tracker, cột Post: Đã post/Chưa)
vercel.json            # rewrites + Cache-Control no-cache cho data.js
```

`HDT_DATA` gồm: `lastUpdated`, `pulse[]`, `weekly{}`, `assetCards[]`, `experts{id: {…, updates[], sources[]}}`, `scorecard[]`.

**5 kênh đang theo dõi** (`experts` là data-driven, thêm kênh chỉ cần thêm key + sửa chữ "N chuyên gia" trong HTML — hiện chỉ còn ĐÚNG 1 chỗ, dòng disclaimer cuối trang Hôm nay):
| id | kênh | nội dung | nhịp |
|---|---|---|---|
| `thai-pham` | @ThaiPhamOfficialVN | Vĩ mô – địa chính trị, hành động danh mục, CK Mỹ/Hàn/Nhật | Video hội viên hàng tuần + bài đăng cộng đồng |
| `azfin` | AzFin Vietnam | Đầu tư giá trị, tư duy và nguyên tắc phân bổ | ~2 bài/tuần |
| `ck-5-phut` | Chứng khoán 5 phút | Nhận định phiên/tuần, thanh khoản, tín hiệu | gần như hàng ngày |
| `quang-dung` | @DauTuChungKhoanCungQuangDung (148K sub, thêm 02/08) | **Định giá doanh nghiệp theo ngành + đọc chính sách vĩ mô** — bóc báo cáo tài chính, không dùng đồ thị | 2–4 bài/tháng, mỗi bài 35–55 phút |
| `lcg-huy` | @lcghuy (thêm 03/08) | **ETF, quỹ chỉ số, quỹ mở** — nói về TỶ TRỌNG phân bổ và tiêu chí chọn quỹ; ở Canada, làm quản lý rủi ro ngân hàng | thứ Tư & Chủ nhật, ~20–35 phút |

- **LCG Huy có ba điểm khác hẳn 4 kênh kia, đừng ép vào khuôn cũ**:
  1. **Không có mốc giá.** Kèo của anh là TỶ TRỌNG ("giữ 50% danh mục"), không entry/target/stop. `tradeLevels` vẫn dựng được nhưng để `entry`/`target`/`stop` là câu chữ mô tả, TUYỆT ĐỐI không bịa số.
  2. **Cố tình không nêu mã ETF trên YouTube** — mã chỉ có trong cộng đồng trả phí. Ghi đúng như vậy, đừng đoán mã (VOO/IWM/VWO…).
  3. **Phụ đề tự động đọc sai tên quỹ viết tắt**: "VSAP" (4 lần) nhiều khả năng là VESAF, "VOF"/"VOOF" (2 lần, hai cách viết) nhiều khả năng là VEOF — nhưng transcript KHÔNG xác nhận, nên để nguyên như phụ đề và ghi chú rõ. Tên đọc chắc chắn: DCDS, VCBF, SSI. Tên ngân hàng anh làm cũng bị đọc thành "ngân hàng B mô" → chỉ ghi "một ngân hàng ở Canada".
- Trong 1 tháng (03/07–03/08/2026) kênh ra 4 video, **chỉ 2 video có nội dung chứng khoán/quỹ chỉ số** (02/08 đổi danh mục, 12/07 bảy tiêu chí quỹ mở). Hai video kia (26/07 việc remote, 05/07 tiền & hạnh phúc) đã đọc transcript và xác nhận không có nội dung đầu tư — vẫn liệt kê trong `sources` kèm ghi chú "đã đọc, bỏ qua" để lần sau không quét lại.
- **Video CK 5 phút có hai dạng, đừng gộp**: livestream phiên (dài 60–70 phút, đầy mã và mốc giá, xen lẫn tán gẫu và giới thiệu sách — phải lọc) và video phân tích ngắn 8–20 phút (một luận điểm duy nhất, thường là bài giá trị nhất tuần). Ngày 17/08 kênh ra cả hai; dựng thành HAI entry riêng vì nội dung không trùng nhau.
- **Video hội viên AzFin (SỐ 147, 19/08) vẫn bị chặn** — yt-dlp báo "available to this channel's members on level: HỘI VIÊN YOUTUBE VÀNG". Cần gói hội viên riêng của AzFin, cookie Profile 90 (gói của Thái Phạm) không mở được. Ghi vào `sources` kèm chú thích, đừng dựng nội dung từ tiêu đề.
- **Kiểm trạng thái video trước khi kết luận "không có phụ đề"**: ngày 21/08 `DN0gsLvwsEA` báo lỗi tải, hoá ra là "This live event will begin in 38 minutes" — livestream chưa diễn ra, không phải thiếu phụ đề. Chạy `yt-dlp --list-subs` để đọc thông báo lỗi thật.
- **Phụ đề CK 5 phút đọc sai tên mã liên tục**: "55 Long"/"Lam Long"/"Lỗ len" = NLG (Nam Long), "GBR" = GVR, "Sco"/"Sabco" = SAB, "Pâ" = không xác định được. Quy tắc: mã nào không suy ra chắc chắn từ ngữ cảnh giá + ngành thì **BỎ**, đừng đoán — ngày 18/08 đã bỏ hai mã ("EGC/EC giữ đất cho BCM" và "con P hỗ trợ 13.5") vì không đủ căn cứ.

- Kênh này tạo ra ca **đa quan điểm mẫu ở S&P 500**: LCG Huy giữ 50% dài hạn vs Thái Phạm đang short. Đã viết `debate` cho `spx` — hai bên nhìn hai thứ khác nhau (cơ cấu chỉ số nhiều năm vs giá và dòng tiền vài tháng), không đặt chung một thước đo được.

- Quang Dũng có phụ đề `vi-orig` đầy đủ; auto-caption sai số lẻ (có đoạn "3.800 → 64.400 tỷ" mâu thuẫn với "3.200 → 4.600 tỷ" cùng một ý) → **chỉ lấy con số xuất hiện nhất quán ít nhất hai lần**.
- Trang **Bối cảnh** (xem 7i) là nơi tổng hợp mảng chính trị/vĩ mô/BĐS — mỗi lần update nhớ rà lại 4 chủ đề theo transcript mới.
- Đã cân nhắc và LOẠI @unicorninvest01 (02/08): không phải vì trùng CK 5 phút (thực tế hai bên bất đồng về margin), mà vì gần như không có kèo chấm được (một video 20 phút chỉ có đúng một mốc số) và transcript nhiễu nặng (Fed→"phé", margin→"côn mơ zin"). Nếu user đổi ý thì thêm ở dạng "góc nhìn đối chiếu", không ép ra kèo.

## LUẬT BẤT DI BẤT DỊCH
1. **Chỉ dựng nội dung từ transcript thật** — KHÔNG BAO GIỜ bịa nhận định từ tiêu đề video. Không có transcript → hỏi user hoặc bỏ qua.
2. **Giữ 7 bài mới nhất/kênh**; riêng Thái Phạm ưu tiên bài HỘI VIÊN (`sourceType` bắt đầu bằng `member`): giữ bài mới nhất + các bài member gần nhất. Sources cũng ~7.
3. **UI cố định, nội dung là dữ liệu** — cập nhật định kỳ chỉ sửa `data.js` (+ Excel), không sửa HTML/CSS trừ khi được yêu cầu.
4. Mọi thay đổi data.js: regenerate bằng node (đọc → sửa object → ghi lại `header + "window.HDT_DATA = " + JSON.stringify(D,null,2) + ";\n"`), validate bằng `node -e "eval(...)"` trước khi commit.

## Quy trình "chạy update" (mỗi lần user yêu cầu)
1. `python3 scan_channels.py --no-transcript` (cần `dangerouslySkipSandbox`/network) → thấy video mới nào.
2. Với video mới nhất mỗi kênh: transcript đã tự tải, hoặc `python3 get_transcript.py <url> --json --out data/transcripts/<id>.json`. AzFin đôi khi chưa có phụ đề → để "Chưa", thử lại lần sau.
3. Đọc transcript ĐẦY ĐỦ → dựng entry: `date` (YYYY-MM-DD), `dateShort`, `timeAgo`, `tab`, `sourceType` (public-video|member-video|member-post), `typeLabel`, `title`, `summary`, `feedChips[{label,sig}]` (sig: up/down/warn/wait), `keyCalls[{tag,value,signal,note}]`, `sections[{title,signal,sigLabel,para,bullets[],levels?}]`.
4. Video hội viên Thái Phạm: thêm `tradeLevels` = mảng NHÓM `[{group:"Chứng khoán Việt Nam", items:[{asset,dir,entry,target,stop,sig,tv}]}]`. `tv` = symbol TradingView (HOSE:TCX, OANDA:XAUUSD, BINANCE:BTCUSDT, NASDAQ:AAPL, TVC:USOIL…). Nhóm chuẩn: Vàng & Bạc / Bitcoin & Crypto / Dầu / Chứng khoán Mỹ / Nhật & Hàn / Chứng khoán Việt Nam.
5. Prepend vào `experts[id].updates`, áp luật 7 bài (member-priority cho thai-pham), cập nhật `sources`, `lastUpdated`.
6. **Scorecard** (`HDT.scorecard[{expert,date,asset,call,result,note}]`): đối chiếu call `pending` với transcript mới → chốt `hit`/`miss` (chuyên gia thường tự xác nhận); thêm call mới kiểm chứng được; **ghi cả call SAI** — đó là thứ làm bảng đáng tin. Kết quả phải có căn cứ transcript, tự thuật ghi "tự thuật".
7. **Pulse strip** (`HDT.pulse`) + **weekly** (`HDT.weekly`): cập nhật theo quan điểm TUẦN MỚI NHẤT — pulse cũ từng mâu thuẫn với weekly (bug đã sửa 15/07), đừng để lặp lại.
7b0. **⚠ TRƯỜNG `mkt` — TÁCH THỊ TRƯỜNG (thêm 05/08 theo yêu cầu user: "tôi muốn chia ra nhận định thị trường Mỹ thị trường Việt Nam riêng biệt hàng ngày, đang bị nói về thị trường VN")**:
Trang Hôm nay có nút chuyển **Việt Nam / Mỹ & thế giới** ở ngay đầu trang. Nó lọc TOÀN BỘ trang: hero, dải chỉ số nhanh, 4 ô đếm vị thế, và cả khối Hành động bên dưới.
- **Mọi phần tử của `HDT.pulse` và `HDT.actions.now/waiting/events` PHẢI có `mkt: 'vn' | 'us'`.** `'us'` nghĩa là Mỹ & thế giới — gồm cả vàng, dầu, Bitcoin, Nhật, Hàn, Úc, trái phiếu Mỹ.
- Quên gắn thì KHÔNG mất mục: `_mktOf()` suy ngược từ Sổ mã theo `region` (`vn`→vn, còn lại→us), không đoán được thì rơi về `'vn'`. Nhưng đừng dựa vào fallback — nhãn kiểu "Danh mục VN", "Bầu cử giữa nhiệm kỳ Mỹ" thì phép dò chịu.
- Bẫy đã gặp: **"Hạn thuế đối ứng Mỹ" phải là `mkt:'vn'`** — nó là thuế Mỹ đánh VÀO Việt Nam nên thuộc thị trường VN, dù tên có chữ "Mỹ".
- Mỗi lần update phải cân xem một thị trường có bị rỗng không. Hiện: now 7vn/3us · waiting 5vn/4us · events 4vn/2us.
- State: `mkt` (khai trong constructor). Biến renderVals: `mktTabs`, `mktLabel`, `curMkt` — nhớ giữ trong khối return.
- **Nút "Xem việc cần làm" và ô đếm vị thế nay CUỘN xuống khối Hành động, không điều hướng.** Từ đợt cải tổ 03/08 khối đó nằm lồng trong `isDashboard` nên `this.go('actions')` cho ra trang trắng — lỗi này tồn tại từ 03/08 đến 05/08 mới phát hiện. Neo cuộn là `[data-act-anchor]`.

7b. **Hành động giá** (`HDT.actions{updated, now[], waiting[], events[]}` — trang "Hành động", thêm 19/07): mỗi lần update phải đồng bộ theo transcript mới — `now[{asset,dir,sig,expertId,detail,level}]` vị thế đang chạy; `waiting[{asset,trigger,action,sig,expertId,note}]` lệnh chờ mức giá; `events[{date|dateLabel,label,note,sig,expertId}]` lịch sự kiện (date ISO → app tự đếm "Còn X ngày", sự kiện đã qua thì XÓA). Gỡ hành động hết hiệu lực, thêm hành động mới, cập nhật `updated`.
7c. **Sổ mã** (`HDT.tickers[]` — trang "Sổ mã", thêm 19/07, cải tổ giao diện kiểu TradingView 30/07, thêm khu vực 30/07): mỗi phần tử `{key, symbol, name, group (Chứng khoán Việt Nam|Quốc tế|Hàng hóa & Crypto — nhãn cũ, ít dùng từ khi có region), region (vn|foreign|kr|jp|commodity — thêm 30/07: vn=toàn bộ CK Việt Nam; kr=Hàn (hiện chỉ Samsung); jp=Nhật (hiện chỉ Nikkei index); foreign=còn lại — chủ yếu Mỹ + Đức (ADS); commodity=Vàng/Dầu/Bạc/BTC. Mã Hàn/Nhật mới thì gán region 'kr'/'jp' tương ứng, ĐỪNG dồn vào 'foreign'), industry (ngành nghề thật, vd "Ngân hàng"/"Bán dẫn"/"Hóa chất & Phân bón" — thêm 30/07), basket (dai-han|ngan-han|tranh — thêm 30/07: dai-han=tích lũy giữ nhiều năm không mốc chốt; ngan-han=có lệnh entry/target/stop cụ thể dù mua hay short; tranh=blacklist đứng ngoài hoàn toàn — quy tắc suy ra nhanh: sig=avoid→thường tranh, sig=down→thường ngan-han, sig=up/wait→tùy ngôn ngữ stance có mốc giá cụ thể (ngan-han) hay "giữ/tích lũy dài hạn/cầm nhiều năm" (dai-han)), tradeMode (spot|margin — thêm 30/07: spot=mua/giữ giao ngay không đòn bẩy — TOÀN BỘ mã CK Việt Nam (thị trường VN không cho short) + RTX/ADS (vị thế mua quốc tế); margin=cần đòn bẩy/short — toàn bộ short quốc tế + Vàng/Dầu/Bạc/BTC vì đều là lệnh entry-stop kiểu CFD/futures, không phải nắm giữ vật chất), tv (symbol TradingView, rỗng = không chart), sig, stance, oneLiner, aliases[], thesis[{expertId,title,body}] (CHỈ từ transcript — **expertId BẮT BUỘC**, xem 7h), orders[{expertId,dir,sig,entry,target,stop,status:active|waiting|done,date,note}], potential (1–5) + potentialNote, caution (1–5) + cautionNote}`. Hiện 54 mã. Mã mới → gán ĐỦ CẢ 5 field mới (region, industry, basket, tradeMode, tv nếu có), đừng thiếu.

7i. **Bối cảnh** (`HDT.context` — trang "Bối cảnh", thêm 03/08 theo yêu cầu user muốn mở rộng sang chính trị nước ngoài, vĩ mô, chính trị–tin Việt Nam và bất động sản VN):
`{updated, note, topics:[{key, label, flag(emoji), headline, sub, sig, stats:[{v,k,sub,sig}], items:[{title, body, expertId, date, sig, tag, extra?}]}]}`
- Hiện 4 chủ đề: `the-gioi` · `vi-mo` · `viet-nam` · `bat-dong-san`. Thêm chủ đề chỉ cần push vào `topics` — UI tự sinh tab.
- **Mọi `item` BẮT BUỘC có `expertId`** — trang này là bối cảnh rút từ transcript chuyên gia, KHÔNG phải bản tin thời sự. Không có chuyên gia nào bàn tới thì để trống, TUYỆT ĐỐI không lấp bằng nguồn ngoài.
- `extra` (tùy chọn) = ô xám phụ, dùng cho chi tiết hội viên bổ sung chưa được chuyên gia xác nhận.
- State: `ctxTopic` (index, khai báo trong constructor). Biến renderVals: `ctxTabs`, `ctxUpdated`, `ctxNote`, `ctxHeadline`, `ctxSub`, `ctxLabel`, `ctxFlag`, `ctxHeadBg`, `ctxHeadColor`, `ctxStats`, `ctxItems`, `ctxItemCount` — đủ 12 biến, nhớ giữ trong khối return.
- User sẽ bổ sung chuyên gia chuyên mảng này sau; khi đó chỉ cần thêm `items` với `expertId` mới.

7j. **⚠ ĐỒNG BỘ SỔ LỆNH — `node sync_orders.js`** (thêm 03/08 sau khi user phát hiện lỗi lớn):
`tradeLevels` trong bài và `tickers[].orders` trong Sổ mã là HAI NƠI CHỨA CÙNG MỘT SỰ THẬT. Trước 03/08 chúng không được đồng bộ → **82 mốc lệnh nêu trong bài mà Sổ mã trống trơn** (vd Thái Phạm bảo short Google 01/08, mã GOOGL không có phiếu lệnh nào; FPT thiếu 4, PANW thiếu 4, MU thiếu 4…). Đã sửa hết và thêm 25 mã còn thiếu → **79 mã · 217 phiếu lệnh**.
- **Sau mỗi lần dựng entry PHẢI chạy** `node sync_orders.js --write`, và trước khi commit chạy `node sync_orders.js` (thoát 0 = sạch).
- Script khớp mã theo `symbol` + `aliases`, suy `status` từ `dir` (chờ→waiting, đã chốt→done, còn lại→active), sắp lệnh mới nhất lên đầu, **sinh lại `views`** cho khối Đa quan điểm (7h).
- Báo "chưa có mã" → phải tạo ticker mới đủ trường, trừ nhóm ngành/chỉ số (danh sách `NOT_A_TICKER` trong script).
- **Bẫy đã dính**: hàm bỏ dấu phải `replace(/đ/g,'d')` TRƯỚC `normalize('NFD')` — chữ `đ` không bị NFD tách ra, nên "Đầu tư công"/"BĐS" lọt qua bộ lọc và bị coi là mã.
- **Bẫy đã dính (03/08, lần 2)**: `NOT_A_TICKER` phải được kiểm **TRƯỚC** `findTk`, không phải sau. `findTk` có bước dò theo TỪNG TỪ nên nuốt nhầm rất dễ: "Đầu tư công" khớp vào mã Dầu (alias `dau`) và đã tạo ra một phiếu lệnh WTI ma của Quang Dũng ngày 04/07; "ETF công ty khai khoáng vàng" của LCG Huy khớp vào mã Vàng dù anh nói rõ là KHÔNG mua vàng. Nhãn lớp tài sản luôn phải thắng phép dò mờ.
- **Ghi call SAI của chuyên gia là việc BẮT BUỘC, không phải tùy chọn (21/08)**: CK 5 phút dự phóng tối 20/08 rằng phiên 21/08 "khả năng cao là một cây pin 3 nữa"; thực tế VN-Index đóng 1.768,12, tăng 33,88 điểm (+1,95%). Đã ghi `miss`. Tương tự, call 18/07 của Thái Phạm "chỉ mua lại BTC ở ~50k" đã ghi `miss` vì chính ông mua lướt ở 65.200 ngày 07/08. **Đừng chỉ chốt hit rồi bỏ qua phần sai** — bảng điểm chỉ đáng tin khi có cả hai.

- **Bẫy đã dính (17/08)**: `statusOf` coi "ĐÃ CHỐT 1/2 — giữ phần còn lại" là `done`, nên vị thế vàng đang mở biến mất khỏi ô đếm "đang chạy" ở trang Hôm nay. **Chốt một phần vẫn là lệnh MỞ** — đã thêm guard `(1 2|mot nua|con lai|phan con|giu phan|giu lai|1 3|2 3)`. Sửa lại 19 phiếu lệnh.
- **Bẫy đã dính (03/08, lần 3)**: chống trùng phải xét cả **lô đang gom**, không chỉ lệnh đã có trong sổ. Nhãn kiểu `"WTI · Brent"` tách thành hai phần cùng trỏ về một mã → sinh hai phiếu lệnh y hệt nhau. Đã dọn 2 bản trùng + 1 phiếu ma; sổ còn 214 → 216 phiếu lệnh sau khi thêm LCG Huy.

7h. **Ghi nguồn + Đa quan điểm** (thêm 02/08 theo yêu cầu user: "kèo của ai nhớ ghi", "chung kèo thì 2 quan điểm khác nhau tôi muốn coi đa quan điểm"):
- **Mọi `thesis` phải có `expertId`** → UI hiện huy hiệu "TP · Thái Phạm" cạnh tiêu đề. Đoạn nào nhắc nhiều chuyên gia thì lấy người được nhắc SỚM NHẤT làm tác giả, người sau là đối chiếu.
- **`views[]` sinh TỰ ĐỘNG từ `orders`** trong script cập nhật (mỗi expertId lấy lệnh đầu tiên; `line` = note + entry + target). KHÔNG viết tay `views` — sửa `orders` rồi chạy lại. Khối chỉ hiện khi ≥2 chuyên gia (`tkHasViews`).
- **`debate` (tùy chọn, viết tay)**: một đoạn "Khác nhau ở đâu" — phải nêu rõ mỗi bên nhìn vào ĐIỀU GÌ (giá/chart vs báo cáo tài chính vs chính sách) và điểm mấu chốt chưa kiểm chứng được. Hiện có ở: HPG, VCB, BID, CTG, HDB, SSI, KDH.
- Biến renderVals: `tkViews`, `tkHasViews`, `tkViewCount`, `tkDebate`, `tkHasDebate`; thesis thêm `hasWho`/`whoInitials`/`whoName`. CSS `.tk-views` là grid auto-fit 260px.
- **HPG là ca bất đồng mẫu**: Thái Phạm + CK 5 phút bảo TRÁNH (giá thủng nền, thép Vin cạnh tranh) vs Quang Dũng bảo TÍCH SẢN (LNST quý 2 +50,6%, Dung Quất 2). `sig` để `warn` và `stance` ghi rõ "CHIA RẼ" thay vì chọn phe.

**Giao diện (30/07 — kiểu TradingView, KHÔNG còn trang chi tiết riêng)**: MỘT màn hình `isTickers` duy nhất (đã xóa `isTicker`/route `screen:'ticker'` cũ) — layout `.tk-layout` = flex 2 cột: `.tk-sidebar` (bên hông, sticky, cuộn riêng, danh sách gọn theo nhóm — click 1 dòng chỉ `setState({tickerKey})`, KHÔNG điều hướng) + `.tk-detail` (ở giữa, luôn hiện đầy đủ thông tin mã đang chọn: hồ sơ kèo, **chart TradingView nhúng tự động** nếu có `tv` — dùng `tkChartEmbedUrl` tính trực tiếp từ `curTicker.tv`, ĐỘC LẬP với cơ chế `chartSym`/`chartOpen` toggle dùng ở trang Chuyên gia để tránh xung đột state, phiếu lệnh, luận điểm, bảng điểm, dòng thời gian). Responsive qua CSS `@media (max-width:860px)`: sidebar co lại thành khối cuộn cao ~380px phía trên, detail full-width phía dưới — không cần JS riêng cho mobile. `curTicker` fallback về `TICKERS[0]` nên luôn có gì đó hiện ở giữa; constructor set `tickerKey` mặc định = mã đầu tiên. **Lưu ý TradingView free-widget không nhận mã HOSE (`tv:"HOSE:xxx"`)** — báo "This symbol is only available on TradingView", đây là giới hạn ngoài (không phải bug); mã Quốc tế/Hàng hóa/Crypto (NASDAQ/NYSE/OANDA/BINANCE/TVC/KRX...) hoạt động bình thường.

**Bộ lọc/nhóm (bản 30/07)**: control chính là **khu vực** — segmented control (`class="tk-segment"`/`tk-seg-btn`, nền track xám + nút active nổi nền trắng/bóng đổ, KHÔNG dùng pill viền thô như trước) với 6 tab: Tất cả · CK Việt Nam · CK nước ngoài · CK Hàn · CK Nhật · Hàng hóa & Crypto (`tkRegionChips`, filter theo `region`, độc lập với mọi thứ khác). Bên dưới là 1 card viền nhạt gồm 3 hàng có nhãn (Xem theo / Giỏ / Loại lệnh) tách bằng đường kẻ mảnh — mỗi hàng cuộn ngang riêng, không còn xếp chồng 3 hàng pill rời rạc như bản đầu (lý do đổi: user chê "cột xem theo hơi thô"). "Xem theo" giờ chỉ còn 2 chế độ (đã bỏ "Thị trường" vì khu vực đã thay thế vai trò đó): Ngành nghề (mặc định, nhóm theo `industry` trong phạm vi khu vực đã lọc, sort count desc) / Giỏ đầu tư (nhóm theo basket, sort BASKET_ORDER dai-han→ngan-han→tranh). "Giỏ" và "Loại lệnh" là bộ lọc độc lập (Tất cả/Dài hạn/Ngắn hạn/Tránh và Tất cả/Spot/Margin), kết hợp được với khu vực + với nhau + với bộ lọc Binance. Mỗi dòng sidebar có chấm màu spot/margin + badge stance; hiệu ứng fade-in so le (`class="tk-pop"` + `animation-delay` theo index, tối đa 23 mốc; tôn trọng `prefers-reduced-motion`).

Mỗi lần update: sửa stance/orders theo transcript mới (lệnh khớp mục tiêu → status done), thêm mã mới khi chuyên gia bắt đầu theo dõi (kèm industry+basket+tradeMode+tv nếu có), cập nhật thesis khi có luận điểm mới, và basket/tradeMode nếu chiến lược đổi (vd từ "chờ mức" sang "giữ dài hạn", hoặc mở vị thế short mới).
7d0. **⚠ BẢN TIN LÀ THỨ HAY BỊ QUÊN NHẤT** (user bắt lỗi 05/08: "ko thấy cập nhật bản tin nhỉ"). Ngày 04/08 và 05/08 tôi cập nhật `briefing.community` nhưng KHÔNG dựng lại khối `briefing` chính — kết quả là trang Bản tin vẫn là Tập 42 và **mâu thuẫn trực tiếp với trang Hôm nay**: hero nói "MUA FULL CỔ PHIẾU" trong khi Bản tin vẫn viết "chờ Index về 1.400 hãy giải ngân". Mỗi lần có video hội viên MỚI của Thái Phạm thì phải dựng lại TOÀN BỘ `briefing`, không chỉ `community`.
- **Thước đo VN-Index nay đọc từ `briefing.gauge`**, không hardcode trong HTML nữa. Trước 05/08 bốn mốc 1686/1585/1400/1868 nằm cứng trong renderVals nên khi mốc giải ngân bị bỏ, thước đo vẫn đứng đó nói "còn X điểm tới vùng giải ngân". Trường: `{title, note, lo, loLabel, mid, midLabel, now, nowLabel, hi, hiLabel}` — nhãn cũng đọc từ dữ liệu nên đổi ý nghĩa thước đo không cần sửa HTML.
- **Số VN-Index lấy THẬT từ VNDirect**, đừng ước lượng: `curl "https://dchart-api.vndirect.com.vn/dchart/history?symbol=VNINDEX&resolution=D&from=<unix>&to=<unix>"`. Ngày 05/08/2026: đóng 1.786,27 · đáy 28/07 là 1.651,20 · đỉnh 90 phiên 1.894,75.
- **KHÔNG nhét thẻ HTML vào chuỗi dữ liệu** (`<b>`) — DCLogic in ra nguyên văn. Cần in đậm thì tách thành trường số riêng rồi bọc `<b>` trong markup.

7d. **Bản tin** (`HDT.briefing` — trang "Bản tin", thêm 27/07): infographic của video Thái Phạm MỚI NHẤT + tổng hợp ý kiến 3 chuyên gia. Cấu trúc: `{updated, expertId, episode, dateLabel, kicker, title, subtitle, lead, timeline[{phase,label,note,state:past|now|next|later,sig}], squeezes[{name,status,headline,headlineNote,body,stats[{v,k}],outcome,sig}], stats[{value,label,delta,sig}], quote{text,source}, todo[{act,what,why,sig}], stockView{title,dateRange,verdict,experts[{expertId,stanceLabel,sig,allocation,view,buy,avoid,quote}],agree[],differ[]}}`. Mỗi lần update: dựng lại theo tập hội viên mới nhất (title/lead phải là luận điểm trung tâm của tập đó, KHÔNG dùng template cũ), `state:'now'` đánh dấu giai đoạn hiện tại trên timeline, `stockView` so sánh quan điểm cổ phiếu VN của cả 3 kênh. Có nút Xuất PDF.
7e0. **Chạy lại đối chiếu Binance định kỳ (làm 17/08)**: Binance liên tục niêm yết thêm hợp đồng TradFi. Cách chạy:
```bash
curl -s "https://fapi.binance.com/fapi/v1/exchangeInfo" -o /tmp/bnc_fut.json
curl -s "https://api.binance.com/api/v3/exchangeInfo" -o /tmp/bnc_spot.json
# lọc symbols có status=TRADING và underlyingSubType chứa "TradFi"
```
Ngày 17/08 có 163 hợp đồng TradFi, đối chiếu ra **6 mã mới**: SanDisk (SNDKUSDT), AMZN, TSLA, KO, WMT, COST. Đáng chú ý nhất là **SanDisk — trước đó là mã DUY NHẤT trong sổ không có chart nào**, nay Binance là nguồn duy nhất xem được. Tổng nay 26 mã có futures, 14 mã có spot.
- **BTC từng bị ghi thiếu**: bản đồ chỉ có `spot:['BTCUSDT']` mà `fut:[]`, khiến chính Bitcoin bị xếp vào nhóm "không có trên Binance". Đã sửa.
- Mã giao dịch được trên Binance futures thì `tradeMode` phải là `margin` (short được, dùng đòn bẩy được) — kiểm lại mỗi lần cập nhật bản đồ.

7e. **Đối chiếu Binance** (`HDT.binance = {updated, totalSpot, totalFut, map:{<tickerKey>:{spot:[cặp],fut:[hợp đồng],kind:'exact'|'proxy',note?}}}` — thêm 27/07): mã nào trong Sổ mã mua bán được trên Binance thì hiện badge vàng "⬡ BINANCE · Spot/Futures" + tooltip, có bộ lọc "Mua được trên Binance", và trang chi tiết có panel giá THẬT (fetch `fapi.binance.com/fapi/v1/ticker/24hr` cho futures, `api.binance.com/api/v3/ticker/24hr` cho spot — CORS mở, lỗi thì im lặng bỏ qua). Chỉ đối chiếu nhóm Quốc tế + Hàng hoá; **KHÔNG đối chiếu mã VN** (trùng ticker với cổ phiếu Mỹ → sai, vd CL = Colgate vs dầu WTI). `kind:'proxy'` cho ETF thay thế (SPY↔S&P500, QQQ↔Nasdaq, EWJ↔Nikkei) — phải hiện nhãn "ETF THAY THẾ". Cập nhật lại khi Binance niêm yết thêm mã: chạy lại đối chiếu bStocks (tag `bStocks`) + TradFi futures (`underlyingSubType` chứa `TradFi`).
7f1. **Tên "Browser N" trong `list_connected_browsers` THAY ĐỔI mỗi phiên — chỉ tin deviceId.** Ngày 18/08 bản có gói hội viên tên "Browser 2"; ngày 21/08 cùng deviceId `0ec0ec9b-2005-407f-a4c2-7b394da1100f` lại hiện tên "Browser 3" (và sau khi select thì báo "Connected to browser Browser 1"). Luôn chọn theo deviceId `0ec0ec9b…`, đừng chọn theo tên.
- Trang bài đăng nạp comment RẤT lười: `javascript_tool` cuộn bằng `window.scrollTo` thường trả về 0 luồng và có lần làm renderer treo (CDP timeout 45s). Cách chạy được: dùng `computer{scroll, repeat:45-50}` trước cho YouTube nạp, rồi mới `javascript_tool` để bóc.

7f0. **⚠ `briefing.community` TỪNG LÀ DỮ LIỆU CHẾT — phát hiện 18/08**: `brCommunity` được tính trong renderVals và có trong khối return từ 28/07, nhưng **KHÔNG HỀ có markup nào đọc nó**. Nghĩa là suốt nhiều đợt update tôi vẫn cần mẫn dựng lại khối comment hội viên mà nó chưa bao giờ hiện lên màn hình. Đã thêm thẻ 12 "Hội viên hỏi" vào bộ bài Bản tin (`_DECK` nay 13 thẻ) + CSS `.dc-cm`/`.dc-cmi`/`.dc-cmq`/`.dc-cmw`/`.dc-cma`/`.dc-cms`/`.dc-cmd`, và bổ sung ba biến `brCmTitle`/`brCmSource`/`brCmDisclaimer`.
- **Bài học chung**: có biến trong khối return KHÔNG có nghĩa là nó được hiển thị. Khi thêm dữ liệu mới, grep tên biến trong phần MARKUP (trước thẻ `<script type="text/x-dc">`), không chỉ trong renderVals.
- Cùng lỗi họ hàng: thước đo VN-Index vẫn hiện khi bấm tab "Mỹ & thế giới", ngay dưới dòng "Mọi thứ bên dưới chỉ tính Mỹ & thế giới". Đã bọc `sc-if value="{{ isMktVn }}"`.

7f. **Comment cộng đồng** (`HDT.briefing.community = {title, source, disclaimer, items[{q, who, likes, status:answered|partial|open|data, statusLabel, a, aSrc}]}` — thêm 28/07): lấy bình luận dưới video Thái Phạm bằng `yt-dlp --cookies-from-browser chrome --write-comments --write-info-json --extractor-args "youtube:comment_sort=top;max_comments=150,all,150,30" -o data/comments/%(id)s <url>`, đọc field `comments` trong `<id>.info.json`. **LUẬT**: bình luận là ý kiến NGƯỜI XEM, TUYỆT ĐỐI không trộn vào `experts[].updates` như nhận định chuyên gia — chỉ đưa vào khối riêng có disclaimer, ghép với quan điểm chuyên gia ĐÃ nêu trong transcript (`a` + `aSrc`), và gắn `status` trung thực: `answered` (đã có quan điểm) / `partial` (mới trả lời một nửa) / `open` (chưa được trả lời) / `data` (số liệu người xem đưa — chưa kiểm chứng). Lọc bỏ comment cảm ơn; ưu tiên comment có số liệu/câu hỏi thực chất. **Giới hạn đã biết**: comment video HỘI VIÊN thường bị chặn (yt-dlp báo members-only dù transcript tải được); bài đăng cộng đồng (community post) yt-dlp không liệt kê được.
7g. **Tuần này — điểm nhấn** (`HDT.weekly.consensus[]`/`divergence[]` — mỗi phần tử vẫn chỉ `{label, detail}`, thêm 30/07): renderVals tự đánh số thứ tự + làm nổi bật phần tử ĐẦU TIÊN mỗi mảng (nền tint màu + viền accent + ★ + chữ to hơn) qua hàm `mkWeeklyItems` — vì vậy khi soạn nội dung mỗi lần update, LUÔN xếp điểm quan trọng/đồng thuận rõ nhất lên đầu mảng `consensus`, và điểm mâu thuẫn lớn nhất lên đầu mảng `divergence` (thứ tự trong data.js quyết định cái gì được nổi bật, không có field riêng để đánh dấu).
7k. **Sổ mã · tab Theo tuần** (`HDT.weekBook`, thêm 24/08): thêm MỘT dòng tuần vào `WEEKS` trong `update_weekbook.js` rồi chạy `node update_weekbook.js` — bảng tự sinh lại từ orders vừa sync. Bắt buộc làm mỗi kỳ (sau bước sync_orders), nếu không bảng sẽ thiếu tuần mới nhất.
8. Excel: đánh dấu video đã post → `Post = "Đã post"` (openpyxl, match videoId trong cột Link).
9. Verify: server `hai-dau-tu` (launch.json, port 5500, python http.server) → mở `http://localhost:5500/Hai Dau Tu.dc.html?v=<timestamp>`; login = gõ ≥4 ký tự bất kỳ (hoặc `localStorage.setItem('hdt_auth','1')`). Check: không lỗi console, không chuỗi `[object`, bài mới hiện đúng tab.
10. Commit + push (Vercel tự deploy). Git hay kẹt lock: `rm -f .git/index.lock .git/HEAD.lock`.

## Gotchas
- Browser cache data.js: đã fix bằng header no-cache trong vercel.json; local dev vẫn nên cache-bust `?v=`.
- **Video hội viên Platinum → PHẢI dùng `--cookies-from-browser "chrome:Profile 90"`** (tìm ra 01/08 sau khi dò cả 82 profile Chrome trên máy). Profile `Default` KHÔNG có gói hội viên → yt-dlp báo "available to this channel's members on level: HỘI VIÊN PLATINUM". `get_transcript.py` và `scan_channels.py` hiện hardcode `chrome` (= Default) nên **tự chạy yt-dlp thủ công với Profile 90** cho video hội viên:
  `yt-dlp --cookies-from-browser "chrome:Profile 90" --write-sub --write-auto-sub --sub-lang vi --sub-format vtt --skip-download -o "/tmp/t/%(id)s" <url>` rồi tự parse VTT → JSON (bỏ dòng timestamp/số/WEBVTT, khử trùng lặp dòng).
  Comment hội viên cũng lấy được bằng profile này: thêm `--write-comments --write-info-json --extractor-args "youtube:comment_sort=top;max_comments=200,all,200,50"`.
  Nếu sau này lại lỗi: dò lại profile bằng vòng lặp qua `ls ~/Library/Application\ Support/Google/Chrome/ | grep -E "^(Default|Profile [0-9]+)$"` — profile đúng sẽ in được tiêu đề video thay vì báo members-only.
- **Thái Phạm KHÔNG trả lời bình luận VIDEO, NHƯNG CÓ trả lời bình luận BÀI ĐĂNG** (kiểm 01–02/08: 0/206 comment video; ngược lại dưới 3 bài đăng 28–31/07 có 6 lượt ông trả lời trực tiếp + 1 bình luận ông tự đăng). Trả lời rất ngắn nhưng là lệnh thật ("mở mới đợi hồi lại rồi đánh tiếp", "Ok", "Số phận của FTNt, Panw,... rồi cũng như apple thôi") → **đây là nội dung chuyên gia, được vào `keyCalls`/`sections`/`scorecard`**, khác với câu hỏi của hội viên (chỉ vào `briefing.community`). Giá trị của comment nằm ở: hội viên bổ sung chi tiết chính sách (vd quyết định tăng tỷ lệ khấu trừ tiền gửi KBNN lên 50% hiệu lực 1/8/2026), số liệu thực tế (lãi suất gửi 9,3%), và lộ ra danh mục + tỉ trọng thật của cộng đồng — dùng cho `briefing.community`, KHÔNG trộn vào `experts[].updates`.
- **Bài đăng cộng đồng — lấy id từ trang `/posts`, ĐỪNG dùng link trong Excel** (link Excel sai id nên trả 404; kiểm 28/07 + 01/08). Cách đúng (tìm ra 02/08):
  1. Xuất cookie: `yt-dlp --cookies-from-browser "chrome:Profile 90" --cookies /tmp/ck90.txt --skip-download --simulate <url video bất kỳ>`
  2. `curl -s -b /tmp/ck90.txt -A "<UA Chrome>" -o /tmp/posts.html "https://www.youtube.com/@ThaiPhamOfficialVN/posts"` → HTTP 200 (~1 MB)
  3. Regex `var ytInitialData\s*=\s*({.*?});</script>` → JSON, đệ quy tìm `backstagePostRenderer`/`sharedPostRenderer`, đọc `postId` · `contentText.runs` · `publishedTimeText` · `voteCount`. Trang trả 10 bài mới nhất, TOÀN VĂN.
  Với id thật thì `youtube.com/post/<id>` trả HTTP 200 bình thường.
- **Comment của BÀI ĐĂNG: KHÔNG lấy được bằng curl/innertube — PHẢI dùng Chrome thật.** Đã thử cạn 02/08: token `FEcomment_post_detail` trong ytInitialData bị percent-encode; gửi lên `youtubei/v1/browse` → 403 PERMISSION_DENIED, lên `youtubei/v1/next` → 1.285 byte chỉ có panel quảng cáo rỗng (dù `logged_in=1` + `Authorization: SAPISIDHASH`); HTML bài đăng không nhúng sẵn comment nào. Cách chạy được:
  1. `list_connected_browsers` → phải hỏi user chọn. **Máy có 2 Chrome: "Browser 1" KHÔNG có gói hội viên (bài đăng trả 404), "Browser 2" (deviceId `0ec0ec9b-…`) MỚI ĐÚNG** — chọn thẳng Browser 2.
  2. `navigate` tới `youtube.com/post/<id>` → cuộn `computer{scroll, repeat:50-60}` để nạp comment (lười tải; thường dừng ở 20–43 luồng, không lấy hết được 100+).
  3. Bung phản hồi: click mọi `#more-replies button, ytd-comment-replies-renderer button`, rồi bóc `ytd-comment-thread-renderer` → `#author-text` + `#content-text`.
  Lưu ý `javascript_tool`: viết biểu thức trần (REPL, có top-level await) — bọc `(async()=>{})()` sẽ trả `{}`; chuỗi trả về dài quá bị cắt nên phải `.slice()` từng khúc. Comment VIDEO vẫn lấy bình thường bằng yt-dlp (xem dòng trên).
- **Bài đăng là nội dung Thái Phạm TỰ VIẾT → được xếp vào `experts['thai-pham'].updates` với `sourceType: 'member-post'`** (badge xanh dương, đã có sẵn trong `SOURCE_TYPE`). Khác hẳn comment hội viên — cái đó chỉ được vào `briefing.community`. Bài đăng viết ngay trong phiên nên thường có lệnh cụ thể hơn video cuối tuần (vd 29/07: "chốt lời 1/2 Dell, Marvell, Amd, Mu, sandisk") → là nguồn tốt nhất để chốt kèo scorecard.
- **Chart mã QUỐC TẾ: dùng CẶP BINANCE, không dùng sàn gốc** (đổi 03/08 theo yêu cầu user: "kèo short CK Mỹ dùng chart TradingView mà cặp của Binance, không phải cặp chính"). Lý do: đó mới là hợp đồng thật sự dùng để short.
  Cách chọn symbol trong renderVals: có `HDT.binance.map[ticker.key]` → `BINANCE:<fut>.P` (hợp đồng vĩnh cửu), không có futures thì `BINANCE:<spot>`, không có ánh xạ thì giữ `ticker.tv` gốc. Trong mảng `fut`/`spot` **ưu tiên cặp kết thúc bằng USDT** (SpaceX có cả SPCXUSD1 và SPCXUSDT — phải lấy USDT).
  Đã kiểm thật trong trình duyệt: AAPL, MU, CRWD, NVDA, SPCX, SAMSUNG đều vẽ được nến trên TradingView. Đầu chart hiện nhãn nguồn ("Binance · hợp đồng AAPLUSDT") và badge "ETF thay thế" nếu `kind==='proxy'` (SPY↔S&P500, QQQ↔Nasdaq, EWJ↔Nikkei).
  **Bẫy đã dính**: WTI có `fut:["BZUSDT","CLUSDT"]` — BZ là Brent, CL mới là WTI. Đã đổi thứ tự trong data.js thành `["CLUSDT","BZUSDT"]`. Khi thêm ánh xạ mới, đặt cặp ĐÚNG với mã lên đầu mảng.
- **Chart mã Việt Nam: TỰ VẼ từ VNDirect, KHÔNG dùng TradingView** (đổi 03/08). Widget miễn phí của TradingView không có bản quyền HOSE/UPCOM nên 30/31 mã VN chỉ hiện "This symbol is only available on TradingView". Nguồn thay thế đã kiểm chứng:
  `https://dchart-api.vndirect.com.vn/dchart/history?symbol=<MÃ>&resolution=D&from=<unix>&to=<unix>` → trả `{t,o,h,l,c,v,s}`, **có `Access-Control-Allow-Origin: *`** nên gọi thẳng từ trình duyệt được. Phủ HOSE, UPCOM, mã nhỏ (BVB) và cả chỉ số (`VNINDEX`, `VN30`).
  Đã loại: TCBS (Cloudflare chặn), TradingView symbol-search (403).
  Cách chạy: `loadVn(sym)` lưu vào state `vnBars`, renderVals dựng `tkVnPath`/`tkVnArea`/`tkVnLevels` rồi vẽ SVG trong đúng hệ màu app. `region==='vn'` → tự vẽ; còn lại → giữ iframe TradingView (`tkHasChart` nay đã loại region vn).
  **Mốc giá chuyên gia vẽ đè lên chart**: rút số từ `orders[].entry/target/stop`, đã lọc bỏ phần trăm, ngày tháng, "tuần/tập/quý/năm/lần/tháng + số", và "số + tỷ/triệu/nghìn" — nếu không lọc thì "kế hoạch tín dụng 36%" của HDB bị vẽ thành mốc giá 36. Chỉ giữ số nằm trong khoảng 0,55–1,8 lần giá hiện tại.
  **Rủi ro**: API nội bộ của VNDirect, không có cam kết công khai. Hỏng thì im lặng — hiện "Chưa lấy được giá cho mã này", không làm vỡ trang. Nếu sau này hỏng: thử lại URL trên bằng curl, kiểm `Access-Control-Allow-Origin`.
- Không tải được video stream của video hội viên (403) — chỉ transcript. Đừng thử vẽ/chụp TradingView tự động (đã thử, quá mong manh) — dùng chart embed có sẵn.
- `scExpert`, `callsExpert`, `assetKey`, `q`, `chartSym`, `tkView`, `tkIndustry`, `tkBasket` là state filter trong renderVals — thêm state mới nhớ khai báo trong constructor.
- **Mọi biến tính trong renderVals phải được thêm vào `return {...}` cuối hàm mới lộ ra template** — quên bước này thì `{{ }}`/`sc-for` âm thầm render rỗng, KHÔNG báo lỗi console (đã dính bug này khi thêm `tkViewChips`/`tkBasketChips` 30/07 — luôn grep tên biến mới trong khối return để xác nhận trước khi verify browser).
- Smoke test renderVals: extract script bằng regex `<script type="text/x-dc"...>`, `node --check`, rồi eval với DCLogic/React/localStorage giả (xem lịch sử commit để có mẫu).
- **Sơ đồ tư duy trang bài (`_MM`/`_score`, thêm 03/08)**: nội dung mỗi bài được chia nhánh theo bảng từ khóa `_MM`, chấm điểm nhãn ×3 + thân bài ×1 (KHÔNG khớp-đầu-tiên). Hai bẫy đã dính:
  1. **`\b` của JS không hiểu chữ có dấu** (`\w` chỉ là `[A-Za-z0-9_]`) → "hàn" khớp trong "ngân hàng", "yên" khớp trong "chuyên gia", đủ để dồn 4 ý của một bài về ETF sang nhánh "Nhật & Hàn". Đã bọc bằng lookaround `(?<!\p{L})(?:…)(?!\p{L})` với cờ `u`. Thêm từ khóa mới thì để chuỗi trần trong `k`, đừng tự viết `\b`.
  2. **Đừng cho từ chung chung vào nhóm vùng miền.** "danh mục"/"tỷ trọng"/"cổ phiếu" từng nằm trong nhóm Chứng khoán Việt Nam và kéo nhầm cả loạt ý sang đó. Chúng nay thuộc nhóm riêng "Danh mục & tỉ trọng".
  Bảng hiện có 12 nhóm; tỷ lệ ý rơi vào "Điểm khác" là thước đo chất lượng — đo bằng cách eval khối `_MM…_score` ngoài node rồi chạy qua toàn bộ `experts[].updates` (34,9% → 12,4% sau lần sửa 03/08). Thêm chuyên gia mảng mới thì kiểm lại con số này.
- **Bullets đã bị bọc thành `{text}`** ở khối chuẩn hóa `updates` (cho markup cũ đọc `{{ b.text }}`). Mọi chỗ đọc `sec.bullets` ở tầng renderVals phải mở ra bằng `typeof b === 'string' ? b : (b && b.text) || ''` — quên là in ra `[object Object]` (đã dính 03/08, 55 mục sections).

- **Shape của `weekly.divergence` và `stockView.differ` KHÁC nhau, dễ viết nhầm (dính 08/09).** Cả hai đều nói về "mâu thuẫn giữa chuyên gia" nên rất dễ dùng chung một cấu trúc — nhưng markup đọc hai kiểu khác hẳn:
  - `weekly.divergence[]` → **object `{label, detail}`**, đi qua `mkWeeklyItems` cùng đường với `weekly.consensus`.
  - `briefing.stockView.differ[]` → **mảng CHUỖI phẳng**, không có `label`/`detail`, và tên field là `differ` chứ KHÔNG phải `disagree`.
  Lần 08/09 tôi viết cả hai thành `{topic, sides:[…]}` — không lỗi console, không lỗi parse, chỉ render rỗng lặng lẽ. Cách bắt: sau khi ghi data.js, so key set từng mảng lồng nhau với bản `git show HEAD:…/data.js` (script đối chiếu ở mục 7). **Đây là biến thể của luật "biến trong return mới lộ ra template": đúng tên field cũng chưa đủ, phải đúng cả hình dạng phần tử.**
- **`updates[]` thiếu `tradeLevels` thì `sync_orders.js` vẫn exit 0** — nó chỉ duyệt bài NÀO CÓ. Bài không có tradeLevels (video phương pháp, video tư vấn cá nhân) nên đặt `tradeLevels: []` tường minh thay vì bỏ trống, để việc "0 phiếu lệnh" là chủ ý chứ không phải quên. Sau mỗi lần dựng bài, chạy vòng kiểm 12 field bắt buộc: date, dateShort, timeAgo, tab, sourceType, typeLabel, title, summary, feedChips, keyCalls, sections, tradeLevels.

- **`briefing.stockView` là DỮ LIỆU CHẾT — phát hiện 14/09 (họ hàng của 7f0).** `brSvTitle`/`brSvRange`/`brSvVerdict`/`brSvExperts`/`brSvAgree`/`brSvDiffer` được tính và trả về trong renderVals nhưng markup (trước dòng `data-dc-script`) KHÔNG đọc biến nào. Bộ bài Bản tin 13 thẻ không có thẻ "ba kênh nhìn thế nào". Nghĩa là mọi lần dựng lại `stockView` từ khi đổi sang deck đều không hiện. `brSqueezes` cũng chết, deck dùng `brSqDeck`. Chưa sửa UI (luật 5) — đã báo user. Cách kiểm nhanh cho mọi khối: `M=$(grep -n data-dc-script "Hai Dau Tu.dc.html" | head -1 | cut -d: -f1); head -n $M "Hai Dau Tu.dc.html" | grep -c <tên biến>` — ra 0 là chết.
- **Ba bước bị bỏ sót LIÊN TIẾP qua nhiều kỳ, phát hiện 14/09**: (1) `update_weekbook.js` — mục 7k ghi "bắt buộc mỗi kỳ" nhưng WEEKS dừng ở w34, tức kỳ 30/08 và 08/09 đều quên; đã bù w35–w37 và dòng tỉ trọng MANUAL. (2) `experts[].sources` — kỳ 08/09 không thêm nguồn nào. (3) Bài đăng cộng đồng Thái Phạm — kỳ 08/09 không quét `/posts`, bỏ lỡ các tút 01–04/09. **Checklist cuối mỗi kỳ, trước commit**: `grep "key: 'w" update_weekbook.js | tail -1` phải là tuần hiện tại · `sources[0].date` của cả 5 kênh phải là ngày kỳ này · đã chạy khối curl `/posts`.
- **Rule 7 bài/kênh khi một kênh ra 10 bài/tuần (CK 5 phút 14/09)**: dựng 7 bài mới nhất, 3 bài cũ nhất ghi vào `sources` với meta "không dựng bài vì vượt giới hạn 7 bài/kênh" và Excel `Post = "Bỏ qua"`. Tút hội viên Thái Phạm ra nhiều bài một ngày thì GỘP theo ngày thành một entry `member-post` ("3 tút trong ngày") để giữ chỗ cho video.
- **Khi đóng lệnh vì chuyên gia đổi chiến lược** (Tập 49: bán hết SSI/TCX/VCK/FPT/PNJ/BCM/SZC, đổi BVB/EIB): sync_orders chỉ THÊM phiếu lệnh mới, không tự đóng phiếu cũ. Phải tự đặt `status:'done'` cho các phiếu `thai-pham` còn mở trên các mã đó (kèm note "ĐÓNG theo Tập N") và sửa `stance`/`sig` của ticker — nếu không Sổ mã vẫn hiện "CẦM" cạnh "BÁN HẾT". Kỳ 14/09 đóng 45 phiếu.

## Tính năng đã có (đừng làm lại)
Tìm kiếm live không dấu (quét cả tickers/bullets) + lịch sử 5 call của mã; trang Tài sản (timeline theo asset); Khuyến nghị (call tracker + nhãn Cũ); Bảng điểm chuyên gia (tỷ lệ đúng/sai + filter); Điểm mua/bán theo nhóm + chart TradingView nhúng; badge MỚI (localStorage `hdt_seen`); PWA; card "Tuần này" (weekly) — điểm Đồng thuận/Mâu thuẫn đánh số, mục đầu tiên mỗi cột nổi bật nền màu + ★ + chữ đậm hơn (thêm 30/07, xem 7g); Sổ mã — toggle Xem theo Thị trường/Ngành nghề/Giỏ đầu tư + bộ lọc giỏ dài/ngắn hạn/tránh + tag ngành-giỏ trên card + hiệu ứng fade-in so le (thêm 30/07).

## Sổ mã · tab "THEO TUẦN" — ma trận danh mục (thêm 24/08, theo mẫu slide của chính Thái Phạm)
Lý do có: user thấy "nhiều kèo quá update không kịp" → cần một bảng gọn thay vì đọc từng phiếu lệnh. Sổ mã nay có 2 tab (state `tkTab`): **Danh sách mã** (giao diện cũ) và **Theo tuần**.
- **TỰ SINH — không viết tay.** `node update_weekbook.js` đọc `tickers[].orders` (lọc `expertId: 'thai-pham'`), gom theo TUẦN × NGÀNH rồi ghi đè `HDT.weekBook`. Vì cùng nguồn dữ liệu nên bảng luôn khớp tab "Danh sách mã".
- **Cập nhật mỗi kỳ = thêm ĐÚNG MỘT DÒNG** vào mảng `WEEKS` đầu file `update_weekbook.js` (`{key,label,from,to,ep}` — nhãn theo cách đánh số của Thái Phạm: Tập 42 = Tuần 31 … Tập 46 = Tuần 34), rồi chạy script. Giữ tối đa `KEEP_WEEKS` = 6 tuần.
- Dữ liệu: `weekBook = { updated, expertId, note, weeks[{key,label,range,ep,now}], markets[{key,label,groups[{key,num,label,tickers,isRatio,cells:{<weekKey>:{t,s,n}}}]}] }`. Hai market: `vn` (13 nhóm) và `us` (20 nhóm). `s` ∈ up/hold/down/warn/wait/avoid. `t` rỗng = "giữ nguyên tuần trước". Mỗi ô hiện lệnh MỚI NHẤT của tối đa 4 mã + "+n mã".
- **Quy tắc phân nhóm: NGÀNH quyết định nhóm, KHU VỰC chỉ quyết định nằm ở tab nào** — nên Samsung/SK Hynix vào "Bộ nhớ & Điện tử" chứ không bị gom thành "Hàn Quốc". Ngành mới xuất hiện → thêm vào `MAP` (nếu chỉ một thị trường) hoặc `MAP_INTL` (ngành dùng chung cả VN lẫn quốc tế, ví dụ Ngân hàng/Bán lẻ/Dầu khí). Ngành chưa map rơi vào "Khác (VN)"/"Khác (quốc tế)" — thấy nhóm "Khác" phình ra là dấu hiệu phải bổ sung MAP.
- Dòng "Tỉ trọng tiền/cổ" không suy được từ lệnh → viết tay trong hằng `MANUAL` của script, cập nhật khi tỉ trọng đổi.
- Bẫy đã dính: ngày trong order là `dd/mm` — **phải đổi sang ISO trước khi so sánh**, nếu không "31/07" > "01/08" theo chuỗi và ô sẽ hiện lệnh cũ. CSS `.wb*`: cột Nhóm sticky trái, cuộn ngang trong `.wb-wrap`; ĐỪNG đặt `position:sticky;top:0` cho thead — chồng với nav app.

## Bộ não / brain (thêm 20/08 — dự án dài hạn "train Claude phân tích", TÁCH RIÊNG khỏi pipeline update)
Hướng đi: Claude tự ra nhận định trước → đối chiếu video Thái Phạm sau → học từ độ lệch. CHƯA gắn vào quy trình "chạy update" — đang giai đoạn hoàn thiện công cụ, chạy tay từng cái.
- `brain/phuong-phap-thai-pham.md` — bản đồ giải phẫu phương pháp (8 trụ cột, 10 skill, kiến trúc 3 tầng).
- **Nhóm J của thư viện = CỔNG CHẶN, chạy TRƯỚC mọi mô hình vĩ mô (thêm 21/08 từ slide giáo trình VWA user đưa)**: (J1) tin kinh tế chỉ lái thị trường khi đủ 4 điều kiện ceteris paribus — không tin nào khác, không xung đột thông tin/số liệu, không tin ngành, không tin cụ thể về mã; (J2) FX và trái phiếu nghe tin kinh tế trực tiếp hơn cổ phiếu và hàng hóa, còn **crypto không nằm trên thang này**. Bỏ qua cổng này đã gây hai lỗi cùng ngày: BTC (bê chuỗi FX/trái phiếu sang crypto) và VN-Index (giải thích phiên FTSE + cơ cấu quỹ bằng khối ngoại). Quy tắc: ≥2 điều kiện J1 vỡ → hạ một bậc tin cậy kèo vĩ mô, ưu tiên tín hiệu cấu trúc giá.
- `brain/thai-pham-mental-models.md` — **thư viện 55 mô hình nhân quả** khảo cổ từ 22 transcript, đối chiếu scorecard (mô hình nào hay đúng/hay sai), từ điển ẩn dụ, checklist "nhận định mù". Đây là tầng 2 của bộ não; file notes thô kèm bên cạnh.
- `fetch_macro.js` — công cụ dữ liệu số 1: kéo lợi suất Mỹ 10Y/30Y (FRED), DXY/vàng/WTI/Brent/USDVND (Yahoo) → `data/macro/<series>.json` (merge theo ngày, idempotent). Chạy `node fetch_macro.js` (cần network, ngoài sandbox; `--offline` để chỉ xem dashboard). Dashboard tự flag theo ngưỡng của thư viện mô hình (30Y vs 5,2%, band dầu 78–90, các mốc mua vàng…) — ngưỡng nằm trong CONFIG đầu file, cập nhật khi kèo đổi. Stooq bị chặn bot, đừng thử lại; Yahoo cần User-Agent giả trình duyệt.
- `fetch_vn_market.js` — công cụ dữ liệu số 2: khối ngoại + độ rộng VN. `node fetch_vn_market.js` (nhanh, ~15 call: tổng mua/bán ròng HOSE theo ngày + net & ROOM NGOẠI của từng mã trong Sổ mã — nguồn `api-finfo.vndirect.com.vn/v4/foreigns`, floor:HOSE~type:STOCK trả đủ 405 mã/1 trang); `--breadth` thêm độ rộng toàn sàn (~400 call dchart, ~1 phút: % trên MA50/MA200, % up/downtrend proxy). Lưu `data/macro/vn_foreign.json` + `vn_breadth.json`. Lần đo đầu 20/08: 25,7% trên MA200 / 60,7% downtrend — xác nhận độc lập câu "73% downtrend" của FunStock. TCBS bị Cloudflare chặn, đừng thử.
- `detect_zones.js` — công cụ dữ liệu số 3: máy nhận diện vùng giá (mô hình G2/E3). `node detect_zones.js HPG GAS` (mã VN qua dchart) · `--yahoo GC=F BTC-USD` (quốc tế) · `--watch` (bảng 1 dòng/mã cho cả Sổ mã VN, ~40s). Tự tìm: vùng hỗ trợ/kháng cự (cụm swing chạm ≥2 lần, band tối đa 3,5%), đỉnh/đáy 52w, MA50/MA200 ngày + MA40 tuần → trend, gap chưa lấp (nam châm kiểu Intel-70), SPRING gần đây, cụm 2-3 đỉnh (chỉ flag khi giá trong 10% của đỉnh 52w). Đã kiểm chứng trùng kèo thật: GAS hỗ trợ 78–79,7 ↔ kèo "đóng trên 80"; FPT kháng cự 75,2–77,7 ↔ "hồi 77-78 thì bán"; vàng 4.285–4.400 ↔ mốc mua 4.340/4.260; STB 76,1 ↔ "vượt 76". Hạn chế: không biết mốc số tròn tâm lý (80 của GAS); phân tích tại chỗ, không lưu file.
- `fetch_polymarket.js` — công cụ dữ liệu số 4 (bộ 4 công cụ ĐỦ): xác suất prediction market (mô hình A8). `node fetch_polymarket.js` kéo mọi market Fed/lãi suất từ gamma-api.polymarket.com (public, lọc từ khóa trên top 500 event theo volume), lưu lịch sử xác suất theo ngày vào `data/macro/polymarket.json` để thấy kỳ vọng dịch chuyển; `--q "gold"` tra ad-hoc; `--offline` xem lại. Dashboard nhắc thẳng kèo pending cần chấm ("Fed không đổi lãi suất cả 2026").
- `calendar.js` — công cụ số 5: LỊCH SỰ KIỆN sắp tới. `node calendar.js` (offline, 60 ngày, `--days N`, `--all`); `node calendar.js --fetch` cập nhật thêm LỊCH KINH TẾ tự động (ForexFactory JSON `nfs.faireconomy.media/ff_calendar_thisweek.json` — tuần hiện tại, impact High, USD/EUR/CNY/JPY, giờ đổi sang VN, cache `data/macro/econ_calendar.json`, tag [KT]; rate-limit 429 khá gắt → chỉ fetch 1 lần/phiên làm việc, dính 429 thì ~10 phút sau tự hết; **Investing.com bị chặn cả curl lẫn Browser pane, đừng thử**). Feed chỉ có TUẦN HIỆN TẠI — muốn tuần sau: mở `forexfactory.com/calendar?week=aug24.2026` bằng Browser pane (trang tự hiện GIỜ VN; bảng render lười — phải front tab + screenshot cho renderer chạy, rồi bóc `tr.calendar__row` bằng javascript_tool: `.calendar__date/__time/__currency/__event`, impact ở class `icon--ff-impact-red|ora|yel`), xong merge tay vào cache (đã làm mẫu 20/08). Nguồn còn lại: `brain/events.json` (lịch TAY — **mỗi lần hd update gặp mốc mới trong transcript thì thêm vào đây**) + mốc định kỳ tự tính (đáo hạn phái sinh VN30 thứ Năm tuần 3, cơ cấu ETF ngoại thứ Sáu tuần 3 cuối quý, nonfarm thứ Sáu đầu tháng). Sự kiện tay `approx:true` tự ẩn khi lịch [KT] có bản chính xác cùng loại (±4 ngày). Trường `keo` trỏ về kèo trong sổ nhận định.
- Chạy nhanh cả bộ trước khi phân tích: `node calendar.js && node fetch_macro.js && node fetch_vn_market.js && node fetch_polymarket.js` (thêm `--breadth` cho vn_market và `detect_zones.js <mã>` khi cần soi kèo cụ thể).
- **⚠ TRANG AI PHẢI ĐƯỢC CẬP NHẬT MỖI LẦN `hd update` (user nhắc 21/08: "ý là có phần bạn tự nhận định ý").** Runbook cũ ghi mảng này "TÁCH RIÊNG khỏi pipeline update" — trên thực tế user coi nó là một phần của lượt chạy. Từ nay: chạy xong 5 kênh thì chạy tiếp `node calendar.js && node fetch_macro.js && node fetch_vn_market.js && node fetch_polymarket.js`, chấm lại bảng hướng, regenerate `D.ai`.
  - **Nhưng giữ đúng tính "mù"**: nếu đã đọc transcript rồi mới viết thì KHÔNG được coi là dự báo mới. Bản viết sau chỉ dùng để CHẤM ĐIỂM + SỬA MÔ HÌNH, và phải ghi rõ điều đó trong `D.ai.note` (đã làm mẫu 21/08). Muốn có nhận định mù thật thì viết TRƯỚC bước đọc transcript.
- **Quy trình đúng đã chạy lần đầu ngày 24/08**: chạy 4 công cụ dữ liệu → viết bảng hướng vào `brain/claude-calls.md` → RỒI MỚI `scan_channels` và đọc transcript → cuối cùng ghi phần "so với chuyên gia" vào từng thẻ `D.ai`. Giữ đúng thứ tự này thì bảng mới là nhận định mù thật.
- **Bài học 30/08 — CHẤM ĐIỂM PHẢI TÁCH HAI TẦNG.** Dòng vàng của bảng #3 kích hoạt điều kiện hỏng (giá thủng MA200), nhưng mô hình nền A9 thì vẫn vận hành ĐÚNG: ngày 28/08 tôi ghi trước "nếu DXY lên tiếp mà vàng vẫn giảm thì A9 vẫn đúng, chỉ là chiều gió đổi" — và đúng vậy, DXY tăng hai tuần liền, vàng giảm 3,7%. Từ nay chấm riêng: (a) dòng hướng đúng/sai, (b) mô hình nền còn vận hành không. Trước nay gộp làm một là sai.
- **Bài học 30/08 — H7b, giới hạn của hedge.** Cùng một tuần, cùng một người: hedge BTC ĂN (điều kiện thả gắn vào SỰ KIỆN — "sau khi Warsh phát biểu"), hedge vàng HỎNG (thả theo cảm nhận giá ở 4.550, mất ~100 giá). Hedge không loại bỏ việc đoán nhịp, nó chỉ dời sang quyết định THẢ. **Chỉ dùng H7 khi đã có sự kiện làm mốc thả.**
- **Bài học 30/08 — đoán mã mới: theo LUẬN ĐIỂM VĨ MÔ mới, không theo ngành đang nóng.** Bốn kỳ liền tôi đoán sai phần ngành của mã mới ông thêm. Ông chọn theo biến vĩ mô vừa xuất hiện trong tuần: tuần này lúa mì lên đỉnh 3 năm → ADM (nông sản Mỹ) và GDX (ETF vàng), không phải chứng khoán hay ngân hàng như tôi đoán.
- **Bài học 28/08 — mô hình H7 "hedge": thứ Claude THIẾU, không phải làm sai.** Bảng mù #3 tự kết luận "không có công cụ nào đo được nhịp của vàng nên bỏ hẳn việc gọi nhịp" — đúng nhưng nghèo. Cùng tuần, Thái Phạm gặp đúng bài toán đó và mở ra nút bấm thứ ba: hedge (mở vị thế đối ứng bằng đúng lượng đang giữ để khóa lãi, rồi thả khi biến số gây nhiễu qua). Ông dùng nó NGƯỢC CHIỀU cho vàng và BTC trong cùng một tút 28/08. Đã dựng thành H7 trong thư viện, kèm ba điều kiện bắt buộc.
- **Bài học 28/08 — đừng suy hướng của chỉ số ra cho cả rổ cổ phiếu.** Tôi để dòng "CK Mỹ đi ngang" rồi ngầm hiểu nhóm công nghệ cũng vậy. S&P tuần này +0,7% trong khi Thái Phạm vẫn viết "chứng Mỹ ngọt" — các lệnh short cổ phiếu riêng lẻ của ông đang ăn dù chỉ số lên. Từ bảng #4 phải tách riêng hai dòng: chỉ số Mỹ, và nhóm công nghệ/cyber Mỹ.
- **Bài học 24/08 — sửa mô hình ở một chỗ KHÔNG tự sửa nó ở mọi chỗ**: tôi đã sửa dòng vàng của mình sang "không đứng ngoài chờ chiết khấu", nhưng khi dự đoán nội dung Tập 46 thì lại đoán Thái Phạm sẽ chốt lời vàng — tức rơi về đúng khung mean-reversion vừa bị chấm sai. Khi mô phỏng người khác cũng phải chạy lại cổng J và các bài học đã ghi.
- **Bài học 21/08 — hai dòng tự chấm SAI, ghi lại vì lỗi có thể lặp**:
  1. **Đừng mượn mốc giá của chuyên gia.** Claude gọi trần BTC ở 71–72k vì lấy ẩn dụ "banana lần 3" của Thái Phạm; `detect_zones.js` chỉ ra cụm cản thật 77.991–79.488 (chạm 5 lần) đã tồn tại từ trước. BTC lên 76.916. **Có công cụ thì phải hỏi công cụ trước.**
  2. **Mô hình mean-reversion (G1, #18) không được áp một mình lên tài sản đang trend mạnh.** Claude bảo chờ vàng về 4.43–4.46; vàng đóng tuần 4.670 không hề nghỉ, kích hoạt đúng điều kiện tự sửa đã ghi trước.
  3. Cả Claude lẫn Thái Phạm cùng sai về BTC; bên đúng là **CK 5 phút** (mẫu hình nêm đáy) — kênh kỹ thuật đang bị bộ não cho trọng số thấp nhất. Tăng trọng số cho tín hiệu mẫu hình giá khi nó mâu thuẫn với luận điểm dòng tiền vĩ mô ở khung 4 tuần.
- **Bài học 14/09 — LUẬT A11 và A12, sau lần dự đoán tệ nhất (1/5).** Tôi đoán Tập 49 "giữ nguyên tuần thứ chín"; ông về 40% tiền mặt. Cay nhất: dòng VN-Index của CHÍNH bảng #4 đã hạ hướng và giá xác nhận (1.830 → 1.788, trên MA50 35% → 28%), nhưng khi mô phỏng ông tôi lại ngoại suy chuỗi "giữ nguyên". **A11: trước khi đoán ông giữ hay đổi, đọc lại bảng hướng của chính mình — dòng nào đã hạ hướng và giá xác nhận thì mặc định ông cũng phản ứng.** **A12: tách lớp** — lớp luận điểm (vàng tăng dài hạn, dầu neo cao tới T11, Vin-Index) hiếm khi đổi; lớp chiến thuật (short hay không short dầu, tỷ trọng tiền, Fed tăng hay không) đổi theo giá và xác suất thị trường. Ông nhường thị trường khi xác suất vượt ~80% ("thị trường thường không sai đâu"), dù trái luận điểm của mình bốn ngày trước.
- **Bài học 08/09 — LUẬT A10: đoán mã mới thì đi XUỐNG chuỗi nhân quả ít nhất hai mắt xích, và hỏi "có phiên bản Mỹ không".** Lần thứ sáu liên tiếp đoán sai ngành của mã mới, và lần này tôi đã BIẾT luật 30/08 (chọn theo luận điểm vĩ mô mới, không theo ngành nóng) mà vẫn sai — vì dừng ở mắt xích ĐẦU TIÊN. Biến mới của tuần là dầu vượt 93; tôi đoán mã dầu khí. Chuỗi thật của ông: dầu tăng → chi phí vận tải tăng → giá lương thực tăng → nông dân tăng sản xuất → **phân bón hưởng lợi** → mua MOS và CTVA. Cay hơn nữa: dòng 8 bảng #4 của chính tôi đã ghi "dầu tăng 20%/tháng đẩy chi phí đầu vào phân đạm" — tôi thấy quan hệ dầu→phân bón nhưng chỉ áp cho DCM/DPM ở Việt Nam, không nghĩ ông sẽ mua phiên bản Mỹ của cùng luận điểm.
- **Bài học 08/09 — ngưỡng band/vùng phải hỏi lại `detect_zones` trong CÙNG kỳ viết bảng.** Dòng dầu của bảng #3 là lần sai nặng nhất từ đầu dự án: sai cả CHIỀU lẫn MÔ HÌNH. Tôi gọi "dầu trôi về mép dưới band 78–90" khi WTI ở 83,34; thực tế là 93,85 (+20%/tháng). Nguyên nhân: đọc band 78–90 từ CONFIG viết tay của `fetch_macro` thay vì chạy `detect_zones` cho CL=F — nếu chạy thì đã thấy giá vẫn trên MA200, tức uptrend chưa hỏng và một tuần giảm 4,3% chỉ là nhịp chỉnh. **Đây là biến thể của đúng lỗi BTC 21/08: dùng số có sẵn thay vì hỏi công cụ. Lần đó mượn số của chuyên gia, lần này mượn số của CHÍNH MÌNH từ quá khứ.** Ngưỡng trong CONFIG chỉ là gợi ý, không phải bằng chứng — và phải cập nhật CONFIG khi band đổi.
- **Bài học 08/09 — viết bảng mù mới KHÔNG cần đợi hết hạn 4 tuần, nếu MÔ HÌNH NỀN đã vỡ.** Bảng #3 mới viết 28/08, hạn tới 25/09, nhưng band dầu mà nó dựa vào đã hết giá trị. Quy tắc: hết hạn thì chấm; mô hình nền vỡ thì viết lại NGAY, và ghi rõ lý do viết sớm trong sổ để lần sau phân biệt được "viết lại vì có cớ" với "viết lại vì sốt ruột".
- `brain/claude-calls.md` — **SỔ NHẬN ĐỊNH MÙ của Claude** (mở 20/08; sản phẩm chính là BẢNG HƯỚNG thị trường ~14 dòng, chấm ĐÚNG/SAI/NỬA mỗi 4 tuần). Quy ước ghi ở đầu file — quan trọng nhất: viết TRƯỚC khi xem nội dung mới của 5 kênh; dời mốc = tự chấm MISS/SAI dòng cũ; chấm trong file này, KHÔNG đưa vào scorecard trên web. Khi chạy `hd update` mà thấy video mới của Thái Phạm: sau khi dựng entry xong, MỞ SỔ NÀY đối chiếu phần "Dự đoán nội dung TP" + các dòng hướng liên quan, ghi kết quả vào entry sổ (lệch phân loại: thiếu dữ liệu / thiếu mô hình / Claude đúng).
- **TRANG "AI" TRÊN WEB (thêm 20/08)** — nav mới giữa Mã và Hồ sơ; nguồn dữ liệu `HDT.ai = { updated, note, entryRef, board[] }`. Mỗi thẻ board: `{ id, group('Thế giới'|'Việt Nam'), asset, arrow('up'|'down'|'side'|'warn'), arrowLabel, headline, confidence, horizon, reasoning[{k:'data'|'model'|'concl', text}], experts[{eid, rel:'agree'|'diff'|'part', relLabel, note}] }`. Bấm thẻ → popup SƠ ĐỒ SUY LUẬN (chuỗi dữ liệu→mô hình→kết luận) + khối SO VỚI CHUYÊN GIA. **Cập nhật hàng ngày/mỗi kỳ**: chạy bộ công cụ → cập nhật bảng hướng trong sổ nhận định → regenerate `D.ai` trong data.js (script mẫu: scratchpad add_ai_page.js — viết lại toàn bộ D.ai, giữ id ổn định). TUYỆT ĐỐI không trộn vào `experts`/`scorecard`/feed chung — trang AI đứng riêng cho đến khi có thành tích. UI đã cố định (state `aiOpen`, tabbar mobile đã sửa grid 5 cột) — chỉ sửa data.js.
